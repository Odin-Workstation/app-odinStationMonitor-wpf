<UserControl x:Class="TacoStationMonitor.Controls.DashboardCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TacoStationMonitor.Controls"
             Height="150">

    <Border
        Margin="10"
        Background="#F5F5F5"
        BorderBrush="#CFCFCF"
        BorderThickness="1"
        CornerRadius="5">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="#3C3F41">

                <TextBlock
    Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:DashboardCard}}}"
    Foreground="White"
    FontSize="18"
    FontWeight="SemiBold"
    HorizontalAlignment="Center"
    VerticalAlignment="Center"/>

            </Border>

            <TextBlock
    Grid.Row="1"
    Text="{Binding Value, RelativeSource={RelativeSource AncestorType={x:Type local:DashboardCard}}}"
    FontSize="34"
    FontWeight="Bold"
    HorizontalAlignment="Center"
    VerticalAlignment="Center"
    TextWrapping="Wrap"/>

        </Grid>

    </Border>

</UserControl>
